{% set name = "pz-rail-sklearn" %}
{% set version = "2.0.3" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pz_rail_sklearn-{{ version }}.tar.gz
  sha256: 204054cd63fc2475d765403f96cd12d7e580c1b5a908cbabe1fc7936226ce4f5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=62
    - setuptools-scm >=6.2
    - pip
  run:
    - python >={{ python_min }}
    - pz-rail-base >=1.0.3
    - scikit-learn
    - pandas
test:
  imports:
    - rail.sklearn
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  license: MIT
  license_file: LICENSE
  home: https://github.com/LSSTDESC/rail_sklearn
  summary: RAIL estimators using scikit-learn regressors
  description: |
    RAIL is a flexible software library providing tools to produce at-scale photometric redshift data products, including uncertainties and summary statistics, and stress-test them under realistically complex systematics. 

extra:
  recipe-maintainers:
    - sschmidt23
    - eacharles
